"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getResolvedTypescriptVersion = exports.getTypescript = void 0;
var getTypescript = function (typescriptPath) {
    var projectTsPath = require.resolve(typescriptPath || 'typescript', {
        paths: [process.cwd()],
    });
    var typescript = require(projectTsPath);
    return typescript;
};
exports.getTypescript = getTypescript;
/**
 * Resolves the TypeScript package version used for ts-loader (project or explicit path).
 * @returns `null` when `typescript` cannot be resolved.
 */
var getResolvedTypescriptVersion = function (typescriptPath) {
    try {
        var version = (0, exports.getTypescript)(typescriptPath).version;
        return version;
    }
    catch (_a) {
        return null;
    }
};
exports.getResolvedTypescriptVersion = getResolvedTypescriptVersion;
